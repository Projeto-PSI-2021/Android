<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tophotels">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hotel"
        android:label="TOPHotels"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity android:name=".vistas.TelaPosLogin"></activity>
        <activity android:name=".vistas.LoginActivity">
        <activity android:name=".HotelSelecionadoActivity"
            android:label="Hotel Selecionado">></activity>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vistas.RegistarContaActivity"
            android:label="@string/CabecalhoRegistarConta"></activity>
    </application>

</manifest>